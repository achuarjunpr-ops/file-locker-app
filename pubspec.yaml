name: file_locker
description: "A secure File Locker Flutter app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.1.6
  encrypt: ^5.0.3
  path_provider: ^2.1.5
  share_plus: ^10.1.3
  local_auth: ^2.3.0
  google_mobile_ads: ^5.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
